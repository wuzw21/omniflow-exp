{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://omniflow.local/schemas/oob/omniflow_function.v3.json",
  "title": "OmniFlow Function",
  "description": "The OmniFlow v2 Function contract with embedded RunLog observations as reusable transfer states.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "function_id",
    "name",
    "description",
    "input_schema",
    "bindings",
    "transfer_states",
    "steps",
    "checker_rules",
    "agent_visible"
  ],
  "properties": {
    "schema_version": {"const": "omniflow.function.v3"},
    "function_id": {"type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_-]{0,63}$"},
    "name": {"type": "string", "minLength": 1, "maxLength": 120},
    "description": {"type": "string", "minLength": 1},
    "input_schema": {"$ref": "#/$defs/input_schema"},
    "bindings": {
      "type": "array",
      "items": {"$ref": "#/$defs/binding"}
    },
    "transfer_states": {
      "type": "object",
      "additionalProperties": {"$ref": "omniflow_run_log.v1.json#/$defs/state"}
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/step"}
    },
    "checker_rules": {
      "type": "array",
      "items": {"$ref": "omniflow_checker_rule.v1.json"}
    },
    "model_handoffs": {
      "type": "array",
      "items": {"$ref": "#/$defs/model_handoff"}
    },
    "agent_visible": {"type": "boolean"}
  },
  "$defs": {
    "input_schema": {
      "type": "object",
      "required": ["type", "properties", "required", "additionalProperties"],
      "properties": {
        "type": {"const": "object"},
        "properties": {"type": "object"},
        "required": {
          "type": "array",
          "items": {"type": "string"},
          "uniqueItems": true
        },
        "additionalProperties": {"const": false}
      }
    },
    "binding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["source", "target"],
      "properties": {
        "source": {"type": "string", "pattern": "^\\$\\.arguments(?:\\.[A-Za-z_][A-Za-z0-9_]*|\\[[0-9]+\\])+$"},
        "target": {"type": "string", "pattern": "^\\$\\.steps\\[[0-9]+\\]\\.action\\.args(?:\\.[A-Za-z_][A-Za-z0-9_]*|\\[[0-9]+\\])+$"}
      }
    },
    "model_handoff": {
      "type": "object",
      "additionalProperties": false,
      "required": ["step_index", "reason"],
      "properties": {
        "step_index": {"type": "integer", "minimum": 0},
        "reason": {"type": "string", "minLength": 1, "maxLength": 500}
      }
    },
    "step": {
      "type": "object",
      "additionalProperties": false,
      "required": ["step_index", "transfer_state_ids", "action"],
      "properties": {
        "step_index": {"type": "integer", "minimum": 0},
        "transfer_state_ids": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string", "minLength": 1}
        },
        "action": {"$ref": "#/$defs/action"}
      }
    },
    "action": {
      "type": "object",
      "additionalProperties": false,
      "required": ["tool", "args"],
      "properties": {
        "tool": {"type": "string", "minLength": 1},
        "args": {"type": "object"}
      }
    }
  }
}

# Diagnostic tools

`manual_androidworld_harness.py` is human-only diagnosis. It must not create
formal results, refresh canonical memory, or replace the unified launcher.
Add diagnostics here only when they are read-only or explicitly separated from
the experiment lifecycle.
